# 🕊️ سجل صمود العائلات - غزة

## نظرة عامة
تطبيق Streamlit احترافي وإنساني لتوثيق ودعم العائلات المنكوبة في قطاع غزة. يوفر واجهة سهلة الاستخدام لإدخال البيانات، تحليلها، وعرضها بشكل تفاعلي.

## المميزات الرئيسية

### 📝 تسجيل البيانات
- نموذج شامل لإدخال بيانات العائلات
- حقول مخصصة للاحتياجات العاجلة
- تسجيل الملاحظات والقصص الإنسانية
- حفظ تلقائي في قاعدة بيانات CSV

### 📊 لوحة الإحصائيات
- مؤشرات رئيسية (عدد العائلات، الأفراد، المناطق)
- رسوم بيانية تفاعلية باستخدام Plotly:
  - توزيع جغرافي
  - أنواع الفقد والأضرار
  - الاحتياجات العاجلة
  - توزيع أحجام العائلات

### 💙 القصص خلف الأرقام
- عرض تفصيلي لقصة كل عائلة
- فلترة حسب المنطقة
- تصميم إنساني يحترم كرامة المتضررين

### 📋 إدارة البيانات
- عرض جدول شامل
- فلترة وترتيب ديناميكي
- تصدير إلى CSV, Excel, JSON

## التصميم

### الألوان العصرية
- **Deep Slate** (#2C3E50): للعناوين والنصوص الرئيسية
- **Muted Teal** (#5F9EA0): للأزرار والعناصر التفاعلية
- **Warm Terracotta** (#E07A5F): للتأكيدات والتحذيرات
- **Soft Cream** (#F8F5F2): للخلفيات

### الخطوط
- خط **Tajawal** الحديث للنصوص العربية
- تنسيق RTL كامل

### التجربة البصرية
- بدون أيقونات - الاعتماد على الخطوط والمسافات
- حواف ناعمة (Border Radius)
- تأثيرات Hover سلسة
- ظلال خفيفة (Box Shadow)

## التثبيت والتشغيل

### المتطلبات
- Python 3.8 أو أحدث
- pip لتثبيت المكتبات

### خطوات التثبيت

1. **نسخ الملفات**
   ```bash
   # تأكد من وجود الملفات التالية:
   # - app.py
   # - requirements.txt
   ```

2. **تثبيت المكتبات**
   ```bash
   pip install -r requirements.txt
   ```

3. **تشغيل التطبيق**
   ```bash
   streamlit run app.py
   ```

4. **فتح التطبيق**
   - سيفتح التطبيق تلقائياً في المتصفح على: `http://localhost:8501`

## البنية التقنية

### معمارية الكود
```
app.py
├── التكوين الأساسي (Page Config)
├── التنسيق المخصص (CSS Injection)
├── DataManager Class
│   ├── load_data()
│   ├── save_data()
│   ├── add_family()
│   └── export_data()
├── واجهة إدخال البيانات
├── لوحة التحليلات
├── قسم القصص
├── عرض البيانات
└── الدالة الرئيسية main()
```

### قاعدة البيانات
- يستخدم CSV كقاعدة بيانات بسيطة
- الملف: `families_data.csv`
- ينشأ تلقائياً عند أول عملية حفظ

### الأعمدة
- `التاريخ`: تاريخ ووقت التسجيل
- `اسم_العائلة`: اسم العائلة
- `عدد_الأفراد`: عدد الأفراد
- `نوع_الفقد`: نوع الضرر
- `الاحتياجات_العاجلة`: قائمة الاحتياجات
- `الموقع_الجغرافي`: المنطقة
- `ملاحظات`: تفاصيل إضافية
- `رقم_التواصل`: رقم الهاتف

## الاستخدام

### 1. تسجيل عائلة جديدة
1. اختر "تسجيل عائلة جديدة" من القائمة
2. املأ البيانات المطلوبة (*)
3. اضغط "تسجيل البيانات"
4. ستظهر رسالة نجاح مع تأثير بصري

### 2. عرض الإحصائيات
1. اختر "لوحة الإحصائيات"
2. استعرض المؤشرات الرئيسية
3. تفاعل مع الرسوم البيانية
4. حلل توزيع الاحتياجات

### 3. قراءة القصص
1. اختر "القصص خلف الأرقام"
2. استخدم الفلتر لتحديد منطقة
3. اقرأ قصة كل عائلة

### 4. تصدير البيانات
1. اختر "عرض البيانات"
2. حدد الأعمدة المطلوبة
3. اختر صيغة التصدير (CSV/Excel/JSON)
4. اضغط زر التحميل

## التخصيص

### تغيير الألوان
عدّل متغيرات CSS في دالة `apply_custom_styling()`:
```css
:root {
    --deep-slate: #2C3E50;
    --muted-teal: #5F9EA0;
    --warm-terracotta: #E07A5F;
}
```

### إضافة حقول جديدة
1. أضف العمود في `_create_empty_dataframe()`
2. أضف حقل الإدخال في `render_data_entry_form()`
3. ضمّنه في `family_data` dictionary

### تخصيص الرسوم البيانية
عدّل في `render_analytics_dashboard()`:
- الألوان: `color_continuous_scale`
- الأنواع: `px.bar`, `px.pie`, etc.
- التنسيق: `update_layout()`

## نصائح للأداء الأمثل

1. **للبيانات الكبيرة**: استخدم قواعد بيانات حقيقية (SQLite, PostgreSQL)
2. **التخزين المؤقت**: استخدم `@st.cache_data` للبيانات التي لا تتغير كثيراً
3. **الأمان**: لا تشارك ملف البيانات علناً - يحتوي معلومات حساسة

## الأمان والخصوصية

⚠️ **تحذير مهم**:
- البيانات المُدخلة حساسة جداً
- لا تشارك ملف `families_data.csv` إلا مع جهات موثوقة
- استخدم التطبيق على خادم آمن للإنتاج
- فعّل HTTPS إذا كان التطبيق على الإنترنت

## الترخيص
هذا المشروع مفتوح المصدر لأغراض إنسانية.

## الدعم والمساهمة
- للمشاكل التقنية: افتح Issue
- للتحسينات: قدّم Pull Request
- للاستفسارات: تواصل مع المطوّر

---

**🤲 معاً نبني الأمل | كل مساهمة تحدث فرقاً**

صُمم بـ ❤️ لخدمة أهلنا في غزة
